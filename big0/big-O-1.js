const boxes = [1,2,3,4,5]

function findSecondBox(array) {
  console.log(array[1])
} 

findSecondBox(boxes)

// whats the big O of the findSecondBox() function
// well its O(1)
// because the function takes constant time to perform the task
// no matter how big array size is, the number of operation of the function is always one